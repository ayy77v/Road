<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Road.LoginPage">
	<ContentPage.Content>
   <Grid
    HorizontalOptions="FillAndExpand" VerticalOptions="Center"
    Padding="30,0"
      >
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Label Text="歡迎來到 Xamarin.Forms"
           FontSize="40"
           Grid.Row="0"
           HorizontalTextAlignment="Center"
           />
    <Label Text="帳號"
           Grid.Row="1"
           />
    <Entry Placeholder="請輸入帳號"
           x:Name="entryAccount"
           Grid.Row="2"
           HorizontalOptions="FillAndExpand"
             />
    <Label Text="密碼" 
           Grid.Row="3"/>
    <Entry Placeholder="請輸入密碼"
           x:Name="entryPassword"
           Grid.Row="4"
           IsPassword="True"  />
    <Button Text="登入"
            x:Name="buttonLogin"
            Grid.Row="5"
            HorizontalOptions="FillAndExpand"
              />
  </Grid>
	</ContentPage.Content>
</ContentPage>
